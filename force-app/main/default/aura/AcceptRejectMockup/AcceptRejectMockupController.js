({
    uwApprove: function(component, event, helper) {
        // Call Helper
        helper.uwApprove(component, event, helper);
    },
    uwReject: function(component, event, helper) {
        // Call Helper
        helper.uwReject(component, event, helper);
    },
    nbApprove: function(component, event, helper) {
        // Call Helper
        helper.nbApprove(component, event, helper);
    },
    nbReject: function(component, event, helper) {
        // Call Helper
        helper.nbReject(component, event, helper);
    },

})